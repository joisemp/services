from django.urls import path
# from ...views import central_admin  # Uncomment when views are created

app_name = "central_admin"

urlpatterns = [
    # Central admin dashboard and management URLs
    # path('', central_admin.dashboard, name='dashboard'),
    # path('users/', central_admin.user_management, name='users'),
    # path('settings/', central_admin.system_settings, name='settings'),
    # Add central admin URLs here
]
